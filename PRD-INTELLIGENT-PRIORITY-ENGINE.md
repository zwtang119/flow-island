# PRD：心流静岛 - 语音驱动日程管理
- **版本**: v4.1 (引入持久化日程仓库)
- **状态**: Active
- **核心目标**: 简化初始交互，通过“语音录入→AI分类→设备聚焦”的极简流程，实现一个**可累积、可管理**的日程高效管理。

---

### **1. 核心理念转变**

**从"临时便利贴"到"持久化仓库"**：系统现在以用户语音输入为主要数据源，AI实时处理并**追加**到用户的日程仓库中。所有日程将被持久化存储在用户本地，确保数据不会因刷新而丢失。

---

### **2. 核心用户流程 (User Journey)**

#### **2.1 初始状态与加载**
- **首次访问**: Web UI和Watchy在无任务时，显示引导文案。
- **后续访问**: Web UI启动时，自动从浏览器的`localStorage`中加载并显示所有已存在的日程。

#### **2.2 语音输入与处理 (累积式)**
- **输入方式**: 用户通过Web UI的“按住说话”按钮进行语音输入。
- **处理逻辑 (AI核心)**:
  1.  AI将语音拆分、分类并提取关键信息，输出结构化的**新日程**JSON数组。
  2.  前端将**新日程**与`localStorage`中已存在的**旧日程**进行合并，形成一个完整的日程列表。
  3.  （可选）在合并时执行简单的去重逻辑，防止完全相同的任务被重复添加。
  4.  将合并后的完整列表**写回`localStorage`**。
- **实时反馈**: Web UI的“我的日程”面板，在语音处理成功后，应立即刷新并展示包含新旧所有日程的完整列表。

#### **2.3 日程管理**
- **单项删除**: 用户可以点击“我的日程”列表中每个日程项旁边的删除图标（例如 "×"），以单独移除该日程。
- **一键清空**: 用户可以点击“我的日程”卡片标题旁的“清空日程”按钮，在确认后，将清除`localStorage`中的所有日程。

#### **2.4 Watchy设备显示**
- **显示逻辑**: 严格遵循基于**完整日程仓库**的“智能排序”规则。
- **焦点区**: 显示**下一个即将发生**的“第一象限(Q1)”或“第二象限(Q2)”事项。
- **上下文区**: 显示紧随其后的**下一个**重要事项(Q1或Q2)。

---

### **3. 界面设计 (UI/UX)**

#### **3.1 Web UI 布局**
```
┌─────────────────┐
│   设备预览        │ ← Watchy 实时显示
│                 │
└─────────────────┘
┌─────────────────┐
│ 🎤 语音输入按钮   │ ← 核心交互入口
│  "按住说话"       │
└─────────────────┘
┌─────────────────┐
│  我的日程 [清空]  │ ← 新增“清空”按钮
│  🔴 Q1: [事项1 [×]]│ ← 新增单项“删除”图标
│  🟢 Q2: [事项2 [×]]│
│  ...              │
└─────────────────┘
```

---

### **4. 数据持久化：客户端仓库 (Client-Side Warehouse)**

- **存储机制**: 用户的完整日程仓库将以JSON数组的形式，存储在浏览器的 **`localStorage`** 中。
- **设计 rationale**:
  - **符合架构**: 该方案完美契合项目“客户端渲染”和“轻后端”的架构哲学，服务器无需任何改动，保持了极高的稳定性和简洁性。
  - **性能最佳**: 所有日程管理操作均在本地完成，无网络延迟，体验流畅。
  - **隐私优先**: 用户的日程数据永远不会离开自己的浏览器。
- **已知妥协**: 这是一个有意识的MVP阶段决策。其代价是日程数据与特定设备和浏览器绑定（无法跨设备同步），且会在用户手动清除浏览器缓存时丢失。

---

### **5. 功能规格 (MVP)**

#### **5.1 范围内 (In Scope)**
- ✅ **语音输入**: 支持通过浏览器Web Speech API进行语音录入。
- ✅ **AI处理**: 实现语音的多事项拆分和四象限实时分类。
- ✅ **日程可视化**: 在Web UI上按四象限清晰展示所有日程。
- ✅ **Watchy智能显示**: 根据智能排序规则，在设备上显示最重要的日程。
- ✅ **持久化存储**: 日程列表通过`localStorage`在用户本地持久化（追加式更新）。
- ✅ **日程管理**:
    - **单项删除**: 用户可以从UI中删除任意一条日程。
    - **一键清空**: 用户可以清空整个日程仓库。

#### **5.2 范围外 (Out of Scope)**
- ❌ **日历同步**: 已被完全移除，作为未来可能的增强功能。
- ❌ **手动编辑日程**: MVP阶段不支持修改已存在的日程内容。
- ❌ **跨设备同步**: 日程数据仅存在于本地浏览器。

### **6. 成功指标 (Success Metrics)**
- **端到端延迟**: 用户语音输入完成到Watchy屏幕更新的平均时间应小于10秒。
- **AI分类准确率**: 在标准测试集上，四象限分类的准确率应大于85%。
- **数据一致性**: Web UI显示的日程列表，必须始终与`localStorage`中的数据和Watchy设备渲染的数据源保持100%一致。

### **7. 已知限制 (Known Limitations)**
- **离线时钟冻结**: 当Watchy设备离开Wi-Fi网络时，屏幕上的时间将停留在最后一次成功同步的时刻。
- **语音识别依赖**: 功能的可用性依赖于用户浏览器的Web Speech API支持情况和麦克风权限。
- **数据易失性**: 用户手动清除浏览器缓存将导致所有日程数据丢失。
